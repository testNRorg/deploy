on:
  workflow_dispatch:

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: deploy
      - uses: actions/checkout@v1
      - run: echo "Hello World!"
